export interface PromptSuite {
  /**
   * project id
   */
  id: string
  /**
   * project name
   */
  name: string
  /**
   * project created time
   */
  created: number
  /**
   * project last updated time
   */
  updated: number
}
